<Window x:Class="LanguageBubble.BubbleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Opacity="0">

    <Window.Resources>
        <!-- Fade out (auto-hide) -->
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="OuterBorder"
            CornerRadius="8"
            Padding="6,6"
            Background="#DD2D2D2D"
            BorderBrush="#44FFFFFF"
            BorderThickness="0.5"
            ClipToBounds="True">
<!-- Carousel: clipped area showing the language row -->
        <Canvas x:Name="CarouselCanvas" ClipToBounds="True">
            <StackPanel x:Name="LanguageRow" Orientation="Horizontal"
                        Canvas.Top="0" Canvas.Left="0">
                <StackPanel.RenderTransform>
                    <TranslateTransform x:Name="RowTranslate" X="0"/>
                </StackPanel.RenderTransform>
            </StackPanel>
        </Canvas>

        <!-- Fallback: single label for non-slide mode -->
        <!-- (managed in code-behind by toggling visibility) -->
    </Border>
</Window>
